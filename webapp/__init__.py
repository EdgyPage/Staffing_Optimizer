"""Local web app for the staffing optimizer.

A FastAPI server (``webapp.app:app``) that wraps the existing engine behind a JSON API and
serves a visual builder, a saved-designs library, and interactive time-step simulation +
analysis pages. Run with ``python -m webapp`` (uvicorn on http://localhost:8000).
"""
